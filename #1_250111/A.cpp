#include <iostream>
#include <vector>
#include <cmath>
using namespace std;

int main() {
    // 高速化のための設定
    cin.tie(nullptr);
    ios::sync_with_stdio(false);
    

    string S;
    cin >> S;

    cout << S[0] << "UPC" << endl;
    
    return 0;
}